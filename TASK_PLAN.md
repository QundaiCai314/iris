# Iris 任务安排（Agent 执行手册 v1.0）

> 日期：2026-09-04。性质：做给 agent 自己看的执行手册。规则：每轮工作先读本手册 → 定位当前步骤 → 执行 → 跑验收 → 更新进度表；验收不过不得进入下一步。用户随时可改。
> 产品定版索引：F:/Iris/README.md | docs/product-spec.md | docs/purchase-timing-research.md（总纲）| docs/research/（R01-R05 + 规范）

## 0. 执行原则（违反 = 返工）
- P1 先研究后定参：任何参数 / 口径必须有出处（Rnn 编号、本手册内联标注、或用户拍板）；无出处参数 = bug。
- P2 验收必须真跑：每步验收命令完成时执行，输出留档（截图 / 终端文本 / 文件）。
- P3 增量可见：每个里程碑结束，用户都能看到可运行的东西（数据 / 决策卡 / 页面）。
- P4 红线（spec 7 与总纲 2.6）：概率要校准；样本 n<30 只给区间；代理数据打标签；回测不承诺；不制造焦虑；不替用户裁决价值。
- P5 沟通与文档用中文；项目文件只放 F:/Iris（禁 C 盘）。
- P6 研究持续：任何「不知道该怎么定」先查证，进研究 backlog（B 编号）或新开 R，不靠猜。

## 1. 决策点（需用户拍板；未决时先做不依赖它的步骤）
- D1 技术栈与依赖（阻塞 M0 后半）：默认 Python 3.12（本机可靠解释器 F:/AstrBot/backend/python/python.exe），标准库优先；需要 pip 包（matplotlib / flask 等）时先确认装到哪个环境、能否装。
- D2 LLM 接入（阻塞 M4）：问卷生成 / 语义解析用哪个模型或 OpenAI 兼容网关（用户可能已有 AstrBot / 其他 key）。无 key 兜底 = 规则化问卷生成（决策树），不阻塞整体。
- D3 界面形态（阻塞 M6 后半）：默认 Web（本地服务，端口沿用旧项目习惯 8123）；先完成 CLI 可验收版，Web 后置。
- D4 演示数据来源（阻塞 M1 后半）：先合成价格序列 + 少量真实价格点（用户手工提供）跑通全链；真实历史价抓取（慢慢买等）按 B01 渐进，不阻塞。

## 2. 里程碑与依赖
- M0 环境与骨架 → M1 数据层 → M2 时序统计引擎（P1）→ M3 事件与生命周期层 → M4 需求澄清问卷（Agent 交互）→ M5 决策引擎（P2 + 买/等/换）→ M6 输出界面与证据链 → M7 回测与答辩收尾。
- 依赖：M2 依赖 M1；M3 依赖 M1；M5 依赖 M2 + M3 + M4；M6 依赖 M5；M7 依赖 M5 + M6。M2 与 M3 可交错推进；M4 的规则化兜底可提前于 LLM 决策。
- 优先序（时间紧时）：M1 > M2 > M5（决策卡）> M4 最小版 > M3 最小版 > M6 > M7。M2/M5 是「金融量化含金量」所在，优先保。

## 3. 进度表（执行时更新）
| 里程碑 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- |
| M0 环境与骨架 | 完成 | 2026-09-04 | 3 tests passed；decisions.md 已登记 D1-D4 |
| M1 数据层 | 完成 | 2026-09-04 | schema v1 + 9 SKU 演示数据（5080 高位/5070 低位剧本）+ 校验器 16 tests |
| M2 时序统计引擎 P1 | 完成 | 2026-09-04 | stats+p1+calib+card：38 tests；3 张真实决策卡 JSON + 校准报告 |
| M3 事件与生命周期层 | 完成 | 2026-09-04 | 事件研究(对照组差分)+生命周期代理曲线5族+异常门控；57 tests；events_report.json |
| M4 需求澄清问卷 | 完成 | 2026-09-04 | 画像 schema + 规则问卷（必需闸门/贴现3小题）+ 商品文本解析；66 tests；M4.3 LLM 增强挂起待 D2 key |
| M5 决策引擎 P2/换 | 完成 | 2026-09-04 | decision.py(期望值分解/裁决/P2网格/条件句) + alternatives.py(R03配对/换购) + 92 tests；demo 三画像全字段卡(5080高位/5070低位/pdd渠道)；A1-A7 待 B05 标定 |
| M6 输出与界面 | 完成 | 2026-09-05 | CLI 全卡渲染(render_card_cli.py) + Web 端到端(server.py+静态前端 8123，resolve->问卷->卡) + 假设编辑重算与历史回看 + smoke 11 项 + 97 tests |
| M7 回测与答辩收尾 | 完成 | 2026-09-05 | M7.1 backtest 213 决策点 + rolling_vol 前视修复；M7.2 redline-check 16 项 14绿2黄1豁免；M7.3 pitch-deck + 双路径演练；M7.4 收尾验证 102 tests + smoke 11；剩 git 首次提交待用户确认 | M7.1 backtest.py 截断历史回测 213 决策点 + 报告；顺带修复 rolling_vol 前视（stats.py）；102 tests |
## 4. 详细任务

### M0 环境与骨架
- M0.1 决策 D1（技术栈 / 依赖）：验证解释器可跑、pip 可用性（python -m pip --version 试运行），结果记入 README「环境」节。
  验收：README 环境节写明解释器路径与依赖策略；pip 可用或已列明不可用及替代方案。
- M0.2 建代码骨架：目录 iris/core、iris/agent、iris/web、tests、scripts、data；每个包 init 文件；tests 放第一个冒烟测试。
  验收：python -m pytest tests -q 有输出（至少 1 个测试通过或明确的跳过原因）；目录树与 TASK_PLAN 一致。
- M0.3 决策记录文件：docs/decisions.md，记录 D1-D4 的结论与日期、理由（引用研究编号）。
  验收：文件存在，4 个决策点各有状态行（已决 / 待用户）。

### M1 数据层（离线价格库）
- M1.1 数据 schema（先文档后代码）：docs/data-schema.md 定义：商品实体（型号 / 品类 / 生命周期参数）、SKU（品牌 / 渠道 / 属性向量 R03）、日价表（OHLC、来源、抓取时间、缺失标记）、事件字典（类型 / 日期 / 可信度）。字段与 spec 6 对齐。
  验收：schema 文档列出每张表 / 字段 / 约束；代码里 dataclass 与文档一致；review 时能指认每个字段的用途。
- M1.2 演示数据集：scripts/make_demo_data.py 生成显卡场景（5080 / 5070 / 5070Ti，2~4 个品牌 SKU，每实体 >= 400 个交易日的日价），内置 2 起换代事件 + 3 年大促日历；用户提供的真实价格点并入并以来源标记。
  验收：生成文件可通过校验器；对任一实体能画出日 K 线（终端 ASCII 或 matplotlib 静态图均可）；数据含明显可识别的涨价段与促销谷。
- M1.3 加载与校验器：iris/core/prices.py 的 load / validate / resample(日/周) 接口；校验规则 = 价格 >0、日期单调、缺失标记、来源与新鲜度字段非空。
  验收：对损坏数据（负价 / 乱序日期 / 缺来源）能报明确错误；单元测试覆盖 5 个以上非法样例。

### M2 时序统计引擎（P1 基础，金融量化核心）
- M2.1 统计原语：iris/core/stats.py：滚动分位（90/365/730 天，线性插值口径写明）、MA20/60、滚动波动率（年化）与波动率分位、IQR / min-max。口径出处：总纲 2.1。
  验收：用手工可算的小序列做属性测试（分位数值对得上）；波动率在常数价格上 = 0。
- M2.2 K 线与 OHLC 聚合：日 → 周重采样，输出 JSON 兼容结构。
  验收：对 demo 数据输出的 OHLC 与手工抽查一致（抽 3 周对账）。
- M2.3 P1 历史模拟：iris/core/p1.py：按「同分位段 + 同事件相位」取样，统计未来 60 天（默认，30/180 辅）较现价降 >=5% 的频率；输出 = 点估计 + 95% 区间（bootstrap 或正态近似，写明口径）+ n；n<30 时只输出区间与方向（总纲 2.2 / R04）。
  验收：合成数据上构造「已知会跌」段，P1 显著高于基准段；n<30 分支触发并正确降级；单元测试覆盖降级路径。
- M2.4 校准模块：iris/core/calib.py：fit / calibration 时间切分、覆盖率统计、Brier；产出校准报告（scripts/calib_report.py）。
  验收：报告输出名义 vs 实际覆盖率；偏差 >0.1 时给出「降级为仅方向」的警示；演示数据上能跑通。
- M2.5 决策卡 JSON schema v1（先于界面定字段）：字段组 = 统计（分位 / 波动率 / 趋势）、P1（点 / 区间 / n / 窗口 / 口径）、事件、替代、决策、依据链。写 iris/core/card.py 与 schema 文档。
  验收：demo 数据上生成一张完整卡片 JSON，校验通过；每个数字有 source_ref 字段（指向数据或 Rnn）。

### M3 事件与生命周期层
- M3.1 事件统计：iris/core/events.py：按 R01 的「对照组差分 + 多事件 pooling」实现事件窗统计（估计窗前 120 天；事件窗 -30~+90 天；输出平均累计降价、置信带、n 起事件）；对照组 = 同品类未事件 SKU 中位。参数出处 R01。
  验收：demo 数据的 2 起换代事件能出「事件后 60 天累计降价均值 + 带 + n」；事件日错位（±3 天）测试用例存在。
- M3.2 生命周期代理曲线族：data/lifecycle/ 按品类存参数曲线（手机 / 笔记本 / 显卡 / 游戏机），参数来源 R02 3 节（量级 + 标注媒体/实证性质）；新品无历史时输出「代理」估计。
  验收：对无历史商品能产出带「代理数据」标签的期望降价曲线；曲线文件有出处注释。
- M3.3 异常状态门控：波动率分位阈值（初值 p90，出处：本手册，待 B01 校准）+ 供需事件标记 → 命中时在卡片上降低日历/代理权重并提示「非常规行情」。
  验收：构造高波动段，门控触发且卡片输出相应提示；阈值写成配置可调。
### M4 需求澄清问卷（Agent 交互）
- M4.1 画像 schema：docs/needs-profile.md：输出画像 = 必需/可选 + 弹性探针答案、用途与使用强度、最晚期限、预算档、二手/平替接受度、等待贴现档（低/中/高，R05 离散选择题）、享乐/实用标签。每字段给可选项枚举。
  验收：schema 评审过；字段可全部映射到 M5 参数。
- M4.2 规则化问卷状态机：iris/agent/needs.py：输入「品类 + 商品特征」→ 决策树出题（必需闸门优先：药/医疗类直接建议渠道比价、走比价分支）；离散选择等待题 3~4 道；答案 → 画像 JSON。出处 R05。
  验收：CLI 脚本走通「显卡 → 6~8 题 → 画像」；必需类目（药）路径不问时机题；画像 JSON 通过 schema。
- M4.3 LLM 语义解析（依赖 D2）：解析商品链接文本 → 商品名/品类/型号抽取；无 LLM 时用手工确认 + 关键词规则。
  验收：D2 未决时本步挂起并在进度表注明；规则版可完成「粘贴文本 → 候选品类 → 用户确认」闭环。
- M4.4 问题个性化：按品类题库生成（R03 属性清单辅助提问：显卡问用途/分辨率/预算等）。
  验收：同一品类两次运行问题集一致可复现；不同品类问题集不同。

### M5 决策引擎（P2 + 买/等/换）
> 状态：完成（2026-09-04，92 tests 全绿）。产物：iris/core/decision.py（decompose / decide / p2_probability / traffic_light / build_conditions / build_decision）、iris/core/alternatives.py（build_rows / best_switch）、tests（decision 17 + alternatives 7 + p1 wait_stats 2）、scripts/make_decision_demo.py（三画像卡：5080 高位→换/等、5070 低位→买、5080 pdd 限期→等）

- M5.1 期望值规则：iris/core/decision.py：输入（P1 结果、事件日历、代理曲线、画像）→ 输出 买/等/换 与期望值分解（G 等待期望收益、U 等待效用损失、R 等待风险；触发带随波动率分位变宽）。出处：总纲 2.5 + R05 3 节。
  验收：构造用例（必需+现价低分位 → 买；可选+高分位+无期限 → 等；替代品显著更优 → 换）各 3 组，输出符合预期且分解表可解释。
- M5.2 P2 概率：简化可解释实现——在期望值分解上对参数扰动（贴现档/期限/波动率）做小规模数值积分或分层采样，输出「现在买是 60 天视野内最优」的概率 + 方法说明。
  验收：P2 与直觉一致（涨价环境必需品 P2 高；等待收益大且无期限 P2 低）；方法说明进入卡片的依据链。
- M5.3 替代品矩阵（R03 降级版）：属性近邻配对比价：跨品牌同核心型号 → 品牌溢价；跨型号（上/下位替代）按性能分对齐 → 每元性能 + 绝对差价。输出低/中/高档位与「贵在哪」拆解。
  验收：demo 数据输出 5080 各品牌对比表与 5070 降档建议，价差拆解数字与手工对账一致；溢价措辞含「值不值由你判断」。
- M5.4 红绿灯与条件句生成：绿=买、黄=等或换、红=别现在买（由期望值差自动映射）；生成「若……则……」条件建议（最晚期限驱动）。
  验收：三色用例覆盖；条件句在用户改期限参数后自动变化；文案过红线检查（无催促/保证词）。

### M6 输出界面与证据链
> 状态：完成（2026-09-05，97 tests + Web 冒烟 11 项全绿）。共享管线 iris/agent/pipeline.py（resolve/build_card/build_events_slim）；M6.1 scripts/render_card_cli.py（周线 sparkline+OHLC、P1 三窗、P2/分解、红绿灯、事件、替代矩阵、依据链，数字均挂 ref）；M6.2 iris/web/server.py + iris/web/static/（离线页面，/api/resolve|answer|recompute|kline|catalog，启动 scripts/serve_web.py）；M6.3 假设编辑器（deadline/强度/贴现档/预算/通胀/供需/平替/用途 -> 重算，服务端会话历史可回看可重置）；必需闸门（药品）无价格库时给明确空态结论。

- M6.1 决策卡渲染（CLI 先行）：终端渲染卡片：K 线（ASCII 或 matplotlib）、双概率、红绿灯、事件日历摘要、替代对比；每项可展开依据（数据口径/样本/n/代理标签/假设）。
  验收：一条端到端路径（用户样例：RTX 5080 8xxx 元场景）输出完整卡片；抽查 5 个数字都能指认 source_ref。
- M6.2 Web 界面（依赖 D3）：本地服务（端口 8123 沿用旧项目习惯）：粘贴链接 → 问答 → 卡片页。静态页面无框架依赖优先。
  验收：浏览器走通全流程；刷新 / 无数据时给出明确空态；不依赖外网资源（离线可用）。
- M6.3 依据展开与假设编辑：用户可改（最晚期限/预算/贴现档）并即时重算（决定项标记）。验收：改期限后 P1/P2/红绿灯联动变化；重算记录可回看。

### M7 回测与答辩收尾
- M7.1 截断历史回测：scripts/backtest.py：用某日期前数据定参，滚动前推，输出命中分布（省的比例/平均节省/最坏情形）。验收：报告含「回测 ≠ 未来保证」声明；结果可复现（固定种子）。
  > 状态：完成（2026-09-05）。实现 iris/core/backtest.py（run_backtest / asof_candidates / future_low / 校准桶）+ scripts/backtest.py（CLI）；
  > 产物 data/demo/backtest_report.json：213 决策点（9 SKU × 3 画像）；建议=等 75 点：命中 53.3%、平均净省 +5.82%、86.7% 等不亏、最坏等亏 13.54%；建议=买 138 点：后悔率 45.6%。
  > P1 样本外校准偏差明显（0-20% 桶低估 +0.21、80-100% 桶高估 -0.41）→ 已登记为 P1 v2 事件相位修正（R01/B04）的研究信号，不改引擎参数。
  > 顺带修复：stats.rolling_vol 原在 asof_idx 语义下把 asof 后收益计入波动率（前视）→ 修复 + test_stats 回归；新增 test_backtest 4 tests。
- M7.2 红线自检清单：docs/redline-check.md 全项自检（校准报告、n<30 降级、代理标签、措辞红线、样本量声明、免责区间）。
  > 状态：完成（2026-09-05）。docs/redline-check.md 定稿：16 项检查 14 绿 + 2 黄（A3 样本内校准 n=5-6 仅流程演示；A4 M7.1 样本外校准偏差 +0.21/-0.41 已披露并挂 P1 v2/B04 研究信号）+ 1 豁免（C2 生命周期代理未接入，无新品场景）；全库禁用词扫描 43 命中 0 违规（仅免责否定句/词表/数学语境/内部文档）。
  验收：清单全绿（或列出已记录的豁免与理由）；demo 文案无「保证/稳赚」类词。
- M7.3 答辩与展示包：docs/pitch-deck.md：方法论一页（R 引用表：R01-R05 + 总纲）、demo 脚本（预设 2 条路径：显卡等与必需品买）、差异化口径（spec 4）。
  > 状态：完成（2026-09-05）。docs/pitch-deck.md v1：一页纸 3 分钟讲稿 + 方法论引用表 + demo 脚本两条路径 + 大赛方向映射 + 数字速查 + Q&A 预演 8 题 + 已知限制；两条 demo 路径已真实演练留档（A：5080 高位 switch/yellow P2=0.36；B：药品必需闸门 3 题 -> 明确空态结论）。验收：demo 两次全流程演练成功；一页纸能在 3 分钟内讲完。
- M7.4 代码收尾：README 更新、测试全绿、TASK_PLAN 进度表收官、研究 backlog 标记后续。
  > 状态：完成（2026-09-05）。README 目录/阶段行收官；清理 frontend 空壳与临时文件、.gitignore 补 _serve_*.txt；最终验证：102 tests + Web 冒烟 11 项 + 服务重启健康。
  > 剩余：git 首次提交（仓库已 init 零提交；按验收执行 commit 前需用户确认）。
  > 后续 backlog 标记：B01 真实历史价、B03 品类图谱、B05 A1-A7 标定、P1 v2 事件相位（R01）、LLM 实跑（D2 key）。
  验收：git（若启用）干净或归档说明；从零按 README 步骤能跑起 demo。

## 5. 全局质量门（每个里程碑结束时执行）
- Q1 测试：新增逻辑有测试且 python -m pytest tests -q 通过（环境受限时写明原因与替代验证）。
- Q2 出处检查：卡片/界面所有参数可指认出处（数据源、Rnn、用户拍板）；无出处 = 未完成。
- Q3 红线检查：P4 五项逐条过（校准、n<30、代理标签、回测披露、措辞）。
- Q4 文档同步：README / 研究索引 / product-spec 不落后于代码。
- Q5 进度表：本手册第 3 节状态与日期更新。

## 6. 风险登记（触发时先报告再处理）
- V1 中文电商反爬 / 无稳定数据：演示走半自动 + 预置数据（spec 6），B01 渐进。
- V2 本机 pip 受限：标准库实现优先；绘图用 ASCII/自绘 SVG；确需第三方包时找用户确认环境。
- V3 无 LLM key：规则化问卷与解析兜底（M4.2/M4.3 规则版），Agent 层后置增强。
- V4 时间预算（黑客松日程未定）：默认优先级 M1 > M2 > M5 > M4 最小 > M3 最小 > M6 > M7；砍单顺序：M6.2 Web → M3 复杂事件 → M4 LLM。
- V5 样本量小被质疑：n<30 纪律 + R04 校准报告 + 答辩口径（概率是条件频率不是预言）。

## 7. 用户验收点（每里程碑结束给用户看什么）
- M0：目录树 + 冒烟测试输出
- M1：schema 文档 + demo 数据（含 K 线图一张）
- M2：P1 卡片 JSON + 校准报告
- M3：换代事件统计结果 + 代理曲线图
- M4：CLI 问答录屏/文本（显卡 6~8 题 → 画像）
- M5：5080 决策卡全字段（P1/P2/红绿灯/替代对比/依据链）
- M6：浏览器端到端演示
- M7：回测报告 + 一页纸答辩稿 + demo 录屏
